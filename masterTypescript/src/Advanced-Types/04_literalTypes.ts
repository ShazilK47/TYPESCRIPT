// # Literal TypeS
//Literal(exact, specific)
//to limit the values that are assigned to a variable>

type Quantity = 50 | 100; //here type literal in action.
let Quantity: Quantity = 50; // now it can be 50 or 100 but nothing else.

//another example
type Metric = "cm" | "inch";
